
exports.up = function(knex) {
    return knex.schema.createTable('cars', function(cars) {
        // .increments() creates auto incrementing primary key named 'id
        cars.increments();

        // 
        cars.string('vin',17).unique().notNullable();
        cars.string('make',1024).notNullable();
        cars.string('model',1024).notNullable();
        cars.integer('mileage',1024).notNullable();
        cars.string('title',1024);
        cars.string('transmission',1024);

    });
};

exports.down = function(knex) {
    return knex.schema.dropTableIfExists('posts');
};

// id	unsigned integer	primary key, auto-increments, generated by database
// vin	string	required, unique
// make	string	required
// model	string	required
// mileage	numeric	required
// title	string	optional
// transmission	string	optional
